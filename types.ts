
export interface ProductSpec {
  series: string;
  power: string;
  stages: string;
  phase: string;
  head: string;
  discharge: string;
  description?: string;
}

export interface MaterialInfo {
  name: string;
  grade: string;
}
